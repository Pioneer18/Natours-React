import React from 'react'
import './Header.css'

const Header = props => (
    <React.Fragment>
        <header className="header">Some text...</header>
    </React.Fragment>
);

export default Header;